# ============================================================
#  lotto-accounting — ตัวอย่างไฟล์ environment
#  คัดลอกเป็น .env.local แล้วเติมค่าจริง (อย่า commit ไฟล์ .env.local)
# ============================================================

# ---------- Supabase ----------
# Project Settings → Data API / API Keys
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# service_role key — ใช้ฝั่ง server เท่านั้น ห้ามใส่ใน NEXT_PUBLIC_*
SUPABASE_SERVICE_ROLE_KEY=
# ชื่อ storage bucket สำหรับเก็บรูปสลิป (ต้องเป็น private bucket)
SUPABASE_STORAGE_BUCKET=slips

# ---------- LINE Login / LIFF (ใช้ล็อกอิน + ฟอร์มกรอกข้อมูล) ----------
# LINE Developers → LINE Login channel → LIFF → LIFF ID
NEXT_PUBLIC_LIFF_ID=
# LINE Login channel ID (ตัวเลข) — ใช้ตรวจสอบ ID token ฝั่ง server
LINE_LOGIN_CHANNEL_ID=

# ---------- LINE Messaging API (ไม่บังคับ — ใช้ดูสรุปยอดในแชท + ส่งการ์ดผลหวย) ----------
LINE_MESSAGING_CHANNEL_SECRET=
LINE_MESSAGING_CHANNEL_ACCESS_TOKEN=
# ---------- การ์ดผลหวยเข้า LINE (หน้า /draws) ----------
# ปลายทาง: groupId ของกลุ่ม (ขึ้นต้น C) หรือ userId ของคนเดียว (ขึ้นต้น U)
# หาค่าได้ 2 ทาง:
#   1. ก๊อปจาก Streamlit Secrets ของ lottery-app → `line_to` (ค่าเดียวกันเป๊ะ)
#   2. พิมพ์ `/id` ในกลุ่มนั้น — OA จะตอบ id กลับมาให้ (เฉพาะคนใน LINE_ADMIN_USER_IDS)
# ⚠️ ไม่ตั้ง = บันทึกผลได้ตามปกติ แค่ไม่ส่งการ์ด (ไม่ใช่ส่งไปมั่ว ๆ)
LINE_REPORT_TO=
# token ที่ใช้ส่งการ์ด — ไม่ตั้ง = ใช้ LINE_MESSAGING_CHANNEL_ACCESS_TOKEN ข้างบน
# ⚠️⚠️ กลุ่มที่รายงานเข้าอยู่ทุกวันนี้เป็นของ OA "Racer" (แอป Streamlit เป็นคนส่ง)
#      ซึ่ง **คนละ OA** กับ channel ของแอปนี้ ⇒ ใช้ token ของแอปนี้ push เข้ากลุ่มนั้น = 403
#      ⇒ ก๊อป `line_channel_access_token` จาก Streamlit Secrets มาใส่ตรงนี้
#      (หรือเชิญ OA ของแอปนี้เข้ากลุ่มแทน แล้วไม่ต้องตั้งตัวนี้เลย)
LINE_REPORT_TOKEN=

# ---------- Google Cloud Vision (ใช้อ่านยอดเงิน/วันที่จากรูป) ----------
# Google Cloud Console → APIs & Services → Credentials → Create credentials → API key
# แล้วเปิดใช้ Cloud Vision API ในโปรเจกต์เดียวกัน
# แนะนำให้จำกัด API key ให้เรียกได้เฉพาะ Cloud Vision API
# โควตา 1,000 ภาพแรกต่อเดือนฟรี หลังจากนั้นคิดตามจำนวนภาพ
GOOGLE_VISION_API_KEY=

# ---------- พอร์ตหวยจากแอป Streamlit (lottery-app) ----------
# secret ที่ฝั่งนั้นใช้ยืนยันตัวตอน POST /api/portfolio/snapshot
# ต้องตรงกับ `snapshot_secret` ใน Streamlit Secrets (หรือ env SNAPSHOT_SECRET ของสคริปต์)
# สุ่มเอาเองยาว ๆ เช่น `openssl rand -hex 32` — ไม่ตั้ง = ปิดรับ snapshot
PORTFOLIO_SNAPSHOT_SECRET=

# ---------- ทั่วไป ----------
# URL ของเว็บที่ deploy แล้ว (ใช้ทำปุ่มลิงก์ในข้อความ LINE)
NEXT_PUBLIC_APP_URL=http://localhost:3000
# โซนเวลาที่ใช้ตัดยอดรายวัน/รายเดือน
APP_TIMEZONE=Asia/Bangkok
# ---------- สิทธิ์การใช้งาน ----------
# LINE userId ของผู้ดูแล คั่นด้วย comma — เข้าหน้า /admin เพื่ออนุมัติสมาชิกได้
# ผู้ดูแลเปิดใช้งานให้ตัวเองอัตโนมัติเสมอ จึงล็อกตัวเองออกจากระบบไม่ได้
# *** ต้องตั้งค่านี้ก่อนรัน migration 0003 ไม่งั้นจะไม่มีใครอนุมัติสมาชิกใหม่ได้ ***
LINE_ADMIN_USER_IDS=

# กันคนนอกแบบตายตัว (ไม่บังคับ) — ถ้าตั้งไว้ คนที่ไม่อยู่ในรายชื่อจะล็อกอินไม่ได้เลย
# ปกติไม่ต้องใช้ เพราะมีระบบอนุมัติสมาชิกที่หน้า /admin อยู่แล้ว
LINE_ALLOWED_USER_IDS=

# ---------- สำหรับ dev เท่านั้น (ห้ามเปิดบน production) ----------
# ตั้ง true เพื่อข้าม LINE Login ตอนพัฒนาในเครื่อง
DEV_AUTH_BYPASS=false
NEXT_PUBLIC_DEV_LINE_USER_ID=
